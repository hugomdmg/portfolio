import Alert from 'react-bootstrap/Alert'

function MainHead() {
    return (
        <Alert variant='secondary'>
            Here you can have a look at the result of my deployed code in aws. This is code I made for my portfolio, but also for fun, so it is focused on little challenges I thought to myself.
        </Alert>
    )
}

export default MainHead;